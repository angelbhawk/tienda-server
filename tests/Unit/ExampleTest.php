<?php

namespace Tests\Unit;

use PHPUnit\Framework\TestCase;
use Alumno;

class ExampleTest extends TestCase
{
    /**
     * A basic test example.
     *
     * @return void
     */
    public function test_nombreCompleto()
    {
        /*
        $alumno = new Alumno();
        $alumno->nombre = "Miguel Angel";
        $alumno->apellidoPaterno = "Murillo";
        $alumno->apellidoMaterno = "Jaimes";

        $this->assertTrue(true);*/
    }
}
